# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# LitVM LiteForge testnet RPC (default already correct, override only if needed)
LITVM_RPC_URL=https://liteforge.rpc.caldera.xyz/http

# litPearls (Illuminated Pearls) ERC-20 contract address
LITPEARLS_CONTRACT_ADDRESS=0xE67adF135ea6a3a658d0DC6aEEcbcA916eBfB49E

# Optional: path to the logo image sent with each balance reply.
# Defaults to assets/litpearls_logo.jpg (already included in the repo).
# LITPEARLS_LOGO_PATH=assets/litpearls_logo.jpg

# Optional: public URL for the logo, used only for inline mode results.
# Defaults to the raw GitHub URL of assets/litpearls_logo.jpg in this repo.
# LITPEARLS_LOGO_URL=https://raw.githubusercontent.com/eloyfel/LitPearlsbot-/main/assets/litpearls_logo.jpg
